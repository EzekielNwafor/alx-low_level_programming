#include "main.h"

/**
 * _isdigit.c - check if numbers are o - 9
 * @c: char to be checked
 *
 * Return: 0 or 1
 */

int _isdigit.c(int c)
{
	if (c >= '0' && c <= '9')
		return (1);
	else
		return (0);
}
